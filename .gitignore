# Rust
/*
/assets/TGA
!/Cargo.toml
!/Cargo.lock
!/src
!/assets
!/.env.example
!/README.md
!/docs
!/AGENTS.md
!/specs
!/.github
!/dev
!/migrations
!/scripts
__pycache__/
!/Dockerfile
!/compose.yaml
# The template for local logs. `compose.override.yaml` itself stays untracked —
# it's a per-machine choice, and compose loads it automatically if present.
!/compose.override.example.yaml
!/.dockerignore
*.example
